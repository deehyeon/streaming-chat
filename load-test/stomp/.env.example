# 서버 URL (필수) - ws:// 프로토콜 제외
SERVER_URL=localhost:8080

# ========== 인증 방식 (택1) ==========
# 방법 1: JWT 토큰 직접 입력
TOKEN=your_jwt_token_here

# 방법 2: 이메일/비밀번호로 자동 로그인 (TOKEN이 없을 경우)
EMAIL=test@example.com
PASSWORD=your_password

# ========== 채팅방 설정 ==========
# 방법 1: 채팅방 ID 직접 지정
ROOM_ID=1

# 방법 2: 새 채팅방 생성 (ROOM_ID가 없을 경우)
CREATE_NEW_ROOM=false
ROOM_NAME=Load Test Room

# 방법 3: 기존 채팅방 목록에서 첫 번째 방 자동 선택
# (ROOM_ID도 없고 CREATE_NEW_ROOM=false인 경우 자동 실행)

# ========== 부하 테스트 설정 ==========
# 메시지 전송 간격 (선택, 기본값: 1000ms)
# 10,000명 테스트 시 서버 부하를 고려하여 조절
MESSAGE_INTERVAL_MS=1000
